services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: bonsight
      POSTGRES_PASSWORD: bonsight
      POSTGRES_DB: bonsight
    ports:
      - "5432:5432"
    volumes:
      - bonsight_pgdata:/var/lib/postgresql/data

volumes:
  bonsight_pgdata:
